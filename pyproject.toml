[project]
name = "ltm-design"
version = "0.1.0"
description = "Long-term memory retrieval experiments for LLMs."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
embeddings = [
  "sentence-transformers>=3.0",
]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
